const multiply = (number, value = 1) => {
  // Escreva aqui a sua função
  return number * value
};

console.log(multiply(8));

console.log(multiply(8, 6)); // teste com duas variaveis, substitui o 1 default